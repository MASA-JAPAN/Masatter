import React from "react";
import Profile from "../components/Profile";

export default function Feed(props: any) {
  console.log(props);

  return (
    <div>
      <Profile />
    </div>
  );
}
